version: 2
jobs:
  build:
    docker:
      - image: microsoft/dotnet:2.2-sdk
    steps:
      - checkout
      - run: find .
      - run: ls -ls
      - run: cd src/
      - run: ls -l
      - run: dotnet restore
      - run: dotnet build